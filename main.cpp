#include "device_modbus.h"
int main() {
    /*
    Modbus_device * ptr_device=new Modbus_device();
    ptr_device->set_rd_cmd();
    ptr_device->run();
    ptr_device->show_humidity();
    ptr_device->show_temperature();
     */
    char * str=new char("fdsg");
    std::cout<<str<<std::endl;
    return 0;
}